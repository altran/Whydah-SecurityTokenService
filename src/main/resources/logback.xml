<?xml version="1.0" encoding="UTF-8" ?>
<configuration>
    <property name="LOG_DIR" value="logs/"/>
    <property name="appName" value="SecurityTokenService"/>

    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <layout class="ch.qos.logback.classic.PatternLayout">
            <Pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</Pattern>
        </layout>
    </appender>


    <!--Daily rolling file appender -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <File>${LOG_DIR}${appName}.log</File>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <FileNamePattern>${LOG_DIR}${appName}-%d{yyyy-MM-dd}.%i.log</FileNamePattern>
            <maxHistory>5</maxHistory>
            <timeBasedFileNamingAndTriggeringPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedFNATP">
                <maxFileSize>20MB</maxFileSize>
            </timeBasedFileNamingAndTriggeringPolicy>
        </rollingPolicy>
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>


    <logger name="com.hazelcast" level="INFO"/>
    <logger name="com.sun.jersey.api.client" level="INFO" />
    <logger name="httpclient.wire" level="WARN" />
    <logger name="org.apache.commons.httpclient" level="WARN" />

    <logger name="net.whydah.token.file.StaticFiles" level="INFO" />
    <logger name="net.whydah.token.config.FreemarkerViewProcessor" level="WARN"/>
    <logger name="net.whydah.token.file.StaticFileViewProcessor" level="INFO" />
    <logger name="net.whydah.token.application.ApplicationToken" level="DEBUG"/>
    <logger name="org.valuereporter.agent.activity" level="WARN"/>

    <logger name="net.whydah" level="TRACE"/>

    <root level="TRACE">
        <!-- <appender-ref ref="STDOUT" />  -->
        <appender-ref ref="FILE"/>
    </root>
</configuration>
